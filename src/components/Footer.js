const Footer = () => {
    return (
        <footer className="footer bg-dark text-secondary pb-2 pt-3">
            <p>We are not responsible for any fractional decomposition that may happen during treatment</p>
        </footer>
    );
};

export default Footer;